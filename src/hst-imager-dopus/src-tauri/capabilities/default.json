{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the DOpus shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
