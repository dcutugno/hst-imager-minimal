{
  "name": "hst-imager-dopus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "smoke": "node scripts/smoke-engine.mjs",
    "prepare-engine": "node scripts/prepare-engine.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run prepare-engine && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.4",
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^5.6.0",
    "svelte-check": "^4.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
